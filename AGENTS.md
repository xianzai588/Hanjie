# AGENTS.md

## 0. 当前阶段约束与无实物路线

当前无法进行真实焊接、试样制备、CMM、金相、硬度、无损检测、洁净度试验和 WPS/PQR。不得虚构实测数据、试样照片、测量结果、设备运行记录或工艺评定结论。

官方固定题允许提交项目研究报告、项目设计图或研究实物。本项目采用“研究报告 + 设计图”路线；没有实物不构成提交形式缺项，但会限制焊接可靠性、焊后位置度、疲劳和洁净度结论的证据等级。当前目标是形成可审计的数字化工艺设计，不是宣称产品已经工程放行。

当前证据按以下口径使用：

- A 类（真实试样、真实焊接、CMM、金相、硬度、NDT、疲劳或洁净度实验）登记为后续工程放行门，不作为本阶段参赛稿的前置条件；
- B 类（可信热—结构模型）只在通过模型准入、网格和边界敏感性检查后使用；未通过准入的结果只能说明趋势、风险或方法探索；
- C 类（工程计算、文献对标、理论推导）用于材料、工艺窗口、承载筛查和组织机理论证，不能写成实物性能；
- D 类（真实几何、CAD、工装可达性、干涉和运动链验证）用于设计可制造性与洁净防护论证，不能替代洁净度和位置度实测；
- E/F 类（控制逻辑、软件验证和构建检查）只能证明数字流程或软件行为，不能证明设备已经完成焊接。

无实物路线下，每个官方维度都必须明确写出：设计方案、可执行的数字或文献验证、当前证据等级、未闭合项和未来工程放行条件。位置度应表述为“设计预算、敏感项和最终 CMM 判据”；异种接头应表述为“材料与组织机理及工艺窗口论证”；疲劳和洁净度应表述为“筛查/防护设计与验证方案”。不得把“条件性达标”写成“实际达标”。


## 1. 项目目标

本仓库的唯一主目标是：

**完成第一届辽宁省大学生材料焊接与铸造工艺设计大赛“中铁山桥杯”焊接工艺设计固定题作品。**

所有开发、计算、仿真、文档、CAD、验证和修改，都必须直接服务于以下问题：

| 核心问题                    | 判断标准                               |
| ----------------------- | ---------------------------------- |
| QT450-10 与 Q235B 如何可靠连接 | 材料、焊材、界面、工艺和组织机理合理                 |
| 如何控制焊后变形                | 能给出 Ø40 孔轴位置度达到 Ø0.05 mm 的设计预算、敏感项和最终 CMM 放行条件 |
| 6P、8P 等方案为什么这样选择        | 有真实工程比较，而不是为了让某个候选“获胜”             |
| 如何避免焊渣和飞溅进入壳体           | 结构、防护、工艺和检查链路完整                    |
| 自动化方案是否可实施              | 工装、焊接路径、控制逻辑和设备接口具有工程合理性           |
| 方案是否经得起评委追问             | 每个重要结论都有明确证据等级和适用边界                |

**仓库工程质量本身不是比赛目标。**

代码、脚本、CI、哈希、可复现构建、文件格式、测试数量等，只是辅助工具，不得反客为主。

---

## 2. 工作优先级

任何任务开始前，先判断它对比赛结果的实际贡献。

优先级固定为：

**P0：直接影响题面硬指标和方案正确性**

例如：

Ø0.05 mm 位置度、异种材料焊接可靠性、裂纹和白口组织风险、焊缝承载与疲劳、6P/8P 选择依据、内腔洁净度、真实几何和工装可行性。

**P1：能够显著增强核心技术证据**

例如：

当前真实几何上的热—结构计算、6P/8P 同条件比较、焊接组织和性能的直接文献依据、关键敏感性分析、工装干涉与退出验证。

**P2：直接服务于最终参赛作品表达**

例如：

修正技术报告中的错误数字、补充真正缺失的技术论证、设计图纸、答辩问题、证据分级和关键结果可视化。

**P3：必要的软件工程维护**

只处理确实妨碍 P0～P2 的问题。

**P4：低价值工程洁癖**

默认不做。

包括但不限于：

哈希冻结、重复 SHA256 校验、无业务意义的构建确定性、时间戳归一化、重复克隆验证、重复 Git LFS 完整性验证、页数门禁、重复 manifest 校验、与本次修改无关的格式整理、纯粹为了“全绿”的测试扩张、无风险修改后的全仓库冒烟测试。

除非这些问题已经实际阻止参赛文件生成、打开、提交或复现关键计算，否则不得主动投入时间。

---

## 3. 禁止把“测试通过”当作“比赛问题解决”

以下结论禁止出现：

“全部测试通过，因此方案可靠。”

“构建成功，因此技术方案成立。”

“哈希一致，因此计算可信。”

“脚本可复现，因此满足比赛要求。”

软件测试只能证明软件行为。

比赛需要证明的是焊接工程问题。

例如：

`pytest` 通过，不能证明焊后位置度 ≤ Ø0.05 mm。

CAD 无干涉，不能证明热态薄裙一定不会漏掉飞溅。

公式计算通过，不能证明异种材料接头不存在白口、未熔合或裂纹。

疲劳公式有裕量，不能证明真实压缩机载荷谱下寿命合格。

必须严格区分：

**软件正确性、模型正确性、工程合理性、物理真实性。**

---

## 4. 测试原则

测试采用“本次修改影响什么，就验证什么”的原则。

| 修改类型          | 默认验证方式                |
| ------------- | --------------------- |
| 文字、说明书措辞、引用修正 | 人工核对相关段落，不跑全量测试       |
| 单个配置值修改       | 运行直接消费该配置的计算或生成脚本     |
| 公式或计算逻辑修改     | 运行对应单元测试 + 复算关键结果     |
| CAD 参数修改      | 重建对应几何 + 检查关键尺寸/干涉    |
| 6P/8P 比较逻辑修改  | 只重跑候选比较及相关报告输出        |
| 热/结构模型修改      | 重跑对应模型并检查物理量、网格/边界敏感性 |
| 提交包构建逻辑修改     | 构建一次最终提交包并检查可打开性      |
| 临近最终比赛提交      | 才允许执行一次完整关键链路验证       |

不得因为“保险”而默认执行所有测试。

不得连续多次运行内容完全相同的测试来获得心理确认。

一次可靠验证已经通过，且输入未变化时，不重复验证。

---

## 5. 默认禁止的低收益动作

除非用户明确要求，或者出现真实故障，否则不要主动进行以下工作：

1. 新增或维护 SHA256 / MD5 发布记录。
2. 为二进制文件时间戳不同专门做确定性构建。
3. 对 Git LFS 反复做 clone、fsck、push dry-run 验证。
4. 每次修改后都跑全仓库 pytest、全量 lint、全量构建。
5. 只修改文档后重建全部 CAD、仿真和技术包。
6. 为页数、文件数量、manifest、文件名增加复杂测试。
7. 为历史版本生成新的 RC、release freeze 或快照记录。
8. 反复执行 `git status`、`git log`、hash 对齐和工作区确认。
9. 为了消除“所有 warning”而修改与比赛无关的代码。
10. 没有发现真实问题时主动“再做一轮审计”“再做一轮冒烟”。
11. 为提高测试数量而新增没有实际失效风险的测试。
12. 在已经完成需求后继续进行无关重构、整理、格式化或工程化。

如果某项工作无法回答：

**“它能具体降低哪个比赛风险，或者增强哪条比赛证据？”**

则默认不做。

---

## 6. 比赛项目的有效验证层级

验证价值从高到低排序如下：

| 等级 | 证据                                    |
| -- | ------------------------------------- |
| A  | 真实试样、真实焊接、CMM、金相、硬度、NDT、疲劳或洁净度实验      |
| B  | 成熟物理模型下的可信热—结构 FE、网格与边界敏感性、真实几何计算     |
| C  | 有明确适用范围的工程计算、文献对标、理论推导                |
| D  | CAD 几何、工装可达性、干涉和运动链验证                 |
| E  | 控制程序、状态机和数字流程验证                       |
| F  | 软件单元测试、lint、构建验证                      |
| G  | hash、时间戳、页数、文件数量、release freeze 等仓库维护 |

资源应优先投入 A～D。

不得因为 F、G 更容易自动执行，就大量投入 F、G 而忽略 A～D。

---

## 7. 仿真工作的停止条件

不得为了“把仿真做得越来越复杂”而无限扩展模型。

任何仿真必须首先回答一个比赛问题。

例如：

“6P 相比 8P 是否真的降低焊后孔轴残余偏移？”

“150 ℃预热与不同热输入对熔合区热循环有什么影响？”

“不同松夹温度对残余变形方向和数量级有什么影响？”

如果当前模型已经无法可靠回答该问题，应：

明确模型边界；

记录失败原因；

选择成熟求解方法、文献对标或其他工程证据；

然后停止继续开发模型本身。

不得把“研究为什么自己的求解器还不收敛”长期作为主线。

除非数值方法本身就是比赛创新点，否则不要自行开发复杂通用求解器。

---

## 8. 禁止为了维护既有结论而做计算

6P、8P、Continuous 等都只是候选方案。

不得因为仓库已经写了“6P 推荐”，就在后续模型、参数选择、权重设计或评价指标中人为维持 6P 获胜。

如果新的可信证据显示：

8P 更稳健；

Continuous 某些情况下更合理；

6P 的热变形收益不足以抵消承载或疲劳损失；

应直接修改最终方案。

**比赛目标是找到合理方案，不是证明历史方案正确。**

---

## 9. 数字必须有唯一权威来源

关键比赛数字不得在 Markdown 中长期手工复制维护。

以下内容应尽可能从权威配置或计算结果自动读取：

焊缝长度、热输入、送丝速度、候选所需许用应力、裕量、位置度预算、6P/8P 切换阈值。

如果同一数字在多个文件出现不一致：

优先修复权威数据链；

不要通过修改所有文字副本来“对齐”。

对于报告中的关键数值，必须能够回答：

“这个数字由哪个输入、哪个公式或哪个计算结果产生？”

---

## 10. 不允许夸大证据

所有结论必须使用与证据等级一致的措辞。

设计分配不能写成实测结果。

代理模型不能写成真实产品性能。

静力结果不能写成焊接残余变形。

材料标称强度不能写成异种焊接接头许用应力。

文献中的类似材料结果不能写成本项目实验结果。

预算闭合不能写成实际位置度已经达标。

软件逻辑验证不能写成自动化设备已经完成。

模型未通过可信度检查时，只能作为趋势、风险或方法探索，不得作为硬指标达标证据。

---

## 11. 每次修改前必须先问的三个问题

开始修改之前，在内部先回答：

**这次修改解决的具体比赛问题是什么？**

**完成后新增了什么此前没有的有效证据？**

**如果不做这项修改，会对最终作品造成什么实际影响？**

如果三个问题都无法给出明确答案，则通常不应该修改。

---

## 12. 每次任务的停止规则

完成用户要求的目标并进行一次与风险匹配的验证后立即停止。

不得继续执行：

“顺便再优化一下。”

“为了保险再跑一遍。”

“再补几个测试。”

“顺便整理仓库。”

“再生成一个 release。”

“再把所有文件 hash 一遍。”

“再全面审计一次。”

除非在验证过程中发现新的、与本任务直接相关的真实问题。

---

## 13. 当前比赛阶段特别约束

当前阶段已经具备较完整的报告、CAD、工装、计算和提交包工程链。

接下来默认不再把以下工作作为主线：

构建系统完善、发布工程、hash、LFS、页数检查、历史 RC 管理、视觉 Demo 扩展、与题面无关的软件功能。

当前研发主线必须优先围绕：

**6P 与 8P 在当前四道真实工艺下的热—结构差异；**

**Ø0.05 mm 位置度设计预算、敏感性与放行判据；**

**QT450-10 / Q235B / NiFe 异种连接的材料与组织机理证据；**

**承载条件筛查、疲劳基线和低热输入之间的设计取舍；**

**内腔洁净结构的可实施性；**

**最终答辩能够直接解释和防守的证据。**

如果新的任务明显偏离这些问题，应主动降低其优先级，而不是因为容易自动完成就执行。

---

## 14. 最终原则

**解决比赛问题，而不是优化仓库本身。**

**验证技术结论，而不是验证文件没有变化。**

**优先获得新的工程证据，而不是制造新的工程流程。**

**一次有效验证胜过十次重复冒烟。**

**发现真实问题就修；没有真实问题就停止。**

在无实物路线下，以设计级证据闭环和工程放行条件完整为完成标准，不以虚构或推测的实测结果填补证据缺口。


