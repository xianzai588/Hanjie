# FLEX-COMPLIANT-4P-S3

本算例由 `simulation/scripts/run_reduced_order.py` 自动生成。

- 目的：比较连接单元数、焊接顺序、结构柔顺性和夹具边界对 `P_sim` 的影响。
- 结果性质：降阶模型相对比较，不是 FE 或 CMM 结果。
- 输入：本目录 `config.yaml`；完整参数边界见 `docs/05-design-assumptions.md`。
- 结果：本目录 `result.csv`。
